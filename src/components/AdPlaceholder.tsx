interface AdPlaceholderProps {
  position: "top" | "bottom" | "sidebar";
  className?: string;
}

export function AdPlaceholder({ position, className = "" }: AdPlaceholderProps) {
  // ADS DISABLED TEMPORARILY AS REQUESTED
  return null;
}
