import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "InScript Keyboard Layout - Complete Hindi Key Mapping",
  description: "Complete InScript keyboard layout reference for Hindi typing. Interactive keyboard with key mapping table. Learn all Hindi character positions on the keyboard.",
};

export default function KeyboardLayoutPageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
