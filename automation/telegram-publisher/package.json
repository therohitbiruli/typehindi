{
  "name": "typehindi-telegram-publisher",
  "version": "1.0.0",
  "description": "Cloudflare Worker that generates and publishes TypeHindi blog posts from Telegram, commits to GitHub, and pings Google Indexing API.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.101.0"
  }
}
