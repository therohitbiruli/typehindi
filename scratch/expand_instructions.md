You are an expert long-form content writer. Your task is to process a government job notification and write a MASSIVE, highly detailed 1500+ word article in ENGLISH ONLY.

Here is your workflow:
1. READ THE URL: Fetch the assigned URL.
2. EXTRACT & EXPAND: Gather the basic details (vacancies, eligibility, dates), and then EXPAND heavily. To reach 1500+ words, you MUST include deep, theoretical sections:
   - Detailed Job Profile & Responsibilities (Day-to-day work)
   - Comprehensive Salary Breakdown & Allowances (DA, HRA, TA)
   - Career Growth & Promotions Hierarchy
   - In-depth Syllabus Analysis (Subject by subject breakdown)
   - Preparation Strategy & Recommended Books
   - Previous Year Cut-off Trends & Exam Difficulty
3. FORMAT: 
   - Use the 22-section markdown structure.
   - Use `- ` for bullets and `1. ` for numbered lists.
   - Format tables in markdown precisely (DO NOT use `\|`).
4. SAVE AS JSON:
   - Create a JSON file at `d:\TypeHindi.in\scratch\expanded_[INDEX].json`.
   - The JSON object must contain EXACTLY these keys:
     - `slug`: string
     - `titleEn`: string (English title)
     - `excerptEn`: string (English short description)
     - `contentEn`: string (The massive 1500+ word English markdown)
     - `content`: string (Just copy `contentEn` into this field for now, the user will auto-translate later)
   - We are NOT generating thumbnails this time. Just save the JSON and finish your turn.
